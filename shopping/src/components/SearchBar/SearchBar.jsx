import React from "react";

const SearchBar = () => {
  return (
    <div >
      <input  className="rounded-sm " type="search" placeholder="search product..." />
    </div>
  );
};

export default SearchBar;
